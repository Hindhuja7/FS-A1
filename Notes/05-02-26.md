1.Arrays.binarySearch() is a static method,because Arrays is class
2.System.out.println() is a static because System is a class,out is the data member

3.String s="5";
String s1=s.concat("6");
s1.concat("7");
S.O.P(s);//5
S.O.P(s1);//56 7 will not concat

4.String s="hello";
String s1="hello";
String s2="hello".intern();
String s3=new String("hello"); //heap
String s4=new String("hello").intern();  //intern will return the string literals->i.e stores in String pool
S.O.P(s==s1);  //true
S.O.P(s==s2); //true
S.O.P(s==s3); //false
S.O.P(s==s4); //true   

5.ArrayList() a=new ArrayList();
a.add("kmit");
a.add(Boolean.TRUE);
a.set(1,21);
S.O.P(a.size());  //2
S.O.P(a); // kmit True 21
S.O.P(a.remove("ngit"));


6. ArrayList l=new ArrayList();
l.add("kmit");
l.add("ngit");
S.O.P(l.contains("m")); //false
S.O.P(l.contains("ngit")); //true


7. ArrayList l=new ArrayList();
    l.add("kmit");
    l.add("ngit");
    String[] s=l.toArray(new String[10]);
    S.O.P(Arrays.toString(s)); //kmit ngit null null null null null null null null

8. String[] s={"kmit","ngit"};
ArrayList l=Arrays.asList(s);
l.set(1,"genesis");
s[0]="india";
for(String s1:s)
{
    S.O.P(s1); //india genesis this is called back collection ->any changes happen to list changes happens in array vice versa
    l.remove(0); //u cannot change the size of array  so u cannot remove element
}
S.O.P(l);