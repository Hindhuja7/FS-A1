varargs should be only at right most of the assignment operator,
we cannot write int test(int x...,int y) ->wrong


final classes in java without keyword are String,wrapper class,System,Date

How do we create a user define immutable -> using private with getter [without setter]

in a parent child class if we have static,instance and constructor block...static parent > static child > instance parent > constructor parent > instance child > constructor child

