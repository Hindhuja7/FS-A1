We can create a class with NO constructor like LocalDate

LocalDateTime is immutable like String

we cannot plus or minus the date and time when it is LocalDate,LocalTime can be modified only date and time at a time using LocalDateTime

DateTimeFormatter is used to represent date,month,yr in a specific format like LONG,SHORT,FULL,MEDIUM

Double d1="3.0";
Double d2=d1.valueOf("3.0d");
if(d1.equals(d2)) //true
if(d1==d2) //false


Integer x=5;
S.O.P(x.intValue()); //?
